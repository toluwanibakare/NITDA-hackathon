# Brand assets

- `palette.txt` — brand colour palette (source of truth for theme colours).
- Served web assets (`logo.jpeg`, `loading.webm`, `hero_shield.jpg`) live in
  `../apps/web/public/` — that is the canonical copy Next.js serves.
  Do not duplicate binaries here.
